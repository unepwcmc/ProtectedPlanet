<!DOCTYPE html>
<html>
<head>
  <title>ProtectedPlanet</title>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>

  <%= csrf_meta_tags %>
</head>

<body>
  <div class="container">
    <header>
      <nav class="nav-main">
        <ul>
          <li class="logo icon">
            <%= link_to "Protected Planet", root_path %>
          </li>
          <li><a href="#">Our Mandate</a></li>
          <li><a href="#">PAS Data</a></li>
          <li><a href="#">Contact Us</a></li>
          <li><a href="#">Blog</a></li>
        </ul>

        <ul class="right">
          <% if user_signed_in? %>
            <li><%= link_to "My Projects", projects_path %></li>
            <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
          <% else %>
            <li><%= link_to "Sign In", new_user_session_path %></li>
          <% end %>

          <li class="search icon">
            <%= link_to "Protected Planet", root_path %>
          </li>
        </ul>
      </nav>

      <div class="search-bar">
        <%= render partial: 'shared/search_form', locals: {query: @query} %>
      </div>

      <%= yield :header %>
    </header>

    <main class="<%= controller_name %>-parent">
      <%= yield %>
    </main>
  </div>

  <footer>
    <div class="footer-inner">
      <div class="footer-logos">
        <a href="http://unep-wcmc.org" title="UNEP-WCMC Website">
          <%= image_tag 'wcmc-footer-logo@2x.png', style: 'width: 58px; height: 33px' %>
        </a>

        <a href="http://iucn.org" title="IUCN Website">
          <%= image_tag 'iucn-footer-logo@2x.png', style: 'width: 39px; height: 39px' %>
        </a>
      </div>

      <div class="footer-copyright">
        <p>&copy; ProtectedPlanet 2014. All rights reserved</p>
      </div>

      <div class="footer-links">
        <%= link_to "About", about_path %>
        <%= link_to "Site Terms of Use", wcmc_terms_path %>
      </div>
    </div>
  </footer>

  <% if Rails.env == 'production' %>
    <%= render 'shared/google_analytics' %>
    <%= render 'shared/mouseflow' %>
    <%= render 'shared/uservoice' %>
  <% end %>

  <script id="download-modal-template" type="text/html">
    <div id="close-modal">X</div>
    <h2>Generating personalised dataset…</h2>

    <p>
      Saved search results can take a while to process in to your
      chosen format for download.
      <% if user_signed_in? %>
        You can wait here, or we'll send you an email when your download
        is ready.
      <% else %>
        Your download will be ready soon, and will appear here when it's
        done.
      <% end %>
    </p>
  </script>

  <script id="download-complete-modal-template" type="text/html">
    <div id="close-modal">X</div>
    <h2>Download available</h2>

    <p>
      Your personalised dataset has been generated and is ready for download.
    </p>

    <p class="link-container">
    </p>
  </script>

  <div id="download-modal" class="modal">
  </div>
</body>
</html>
