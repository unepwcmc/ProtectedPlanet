<p>
  Hi,
</p>

<p>
  Your recently started download on <a href="<%= root_url %>">Protected
  Planet</a> is now complete.
</p>

<%# A download is now generated for a single requested format and the zip is %>
<%# named after the download itself, so there is one file to link to. %>
<ul>
  <li><%= link_to("Download your file", Download.link_to(@filename)) %></li>
</ul>
