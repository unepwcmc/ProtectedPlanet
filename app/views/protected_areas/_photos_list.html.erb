<% if images.count > 0 %>
  <ul class="protected-area-photos">
    <% images.each do |image| %>
      <li>
        <a href="<%= image.details_url %>"><img src="<%= image.url %>"></a>
      </li>
    <% end %>
  </ul>
<% else %>
  <%= t('pa_images_not_found') %>
<% end %>
