<li class='result'>
  <%= image_tag "world@2x.png", style: "width: 40px; height: 40px" %>
  <div>
    <h3>
      <a href="<%= regional_stats_path(iso: model.iso) %>"><%= model.name %></a> | 
      <small><%= model.protected_areas.count %> Protected Areas in this country</small>
    </h3>

    <p>
      <%= StatisticPresenter.new(model).percentage_pa_cover %>% of this country is covered by Protected Areas
    </p>
  </div>
</li>
