<div class="card--stats card--stats--half">
  <h2><%= t('stats.iucn-categories.title') %></h2>

  <ul class="list--underline">
    <% iucn_categories.each do |category| %>
      <li class="list__li">
        <i class="list__icon"></i>
        <span class="list__title"><%= category['iucn_category_name'] %></span>
        <span class="list__value"><%= category['count'] %>, <%= category['percentage'].to_f.round(2) %>%</span>
        <%= link_to 'View list', '', title: '', class: 'list__a' %>
      </li>
    <% end %>
  </ul>
</div>

<!-- search_url("#{location.class.name.downcase}" => location.name, iucn_category: row["iucn_category_name"], main: location.class.name.downcase.to_sym) -->