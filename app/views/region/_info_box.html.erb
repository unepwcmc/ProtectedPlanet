<aside class="info-box info-box--fill-color">
  <section class="info-box__column">
    <h2 class="info-box__header header-with-delimiter header-with-delimiter--bold">Citation</h2>
    <p class="info-box__body">
    UNEP-WCMC (<%= Date.today.year %>). Protected Area Profile for <%= @region.name %> from the World Database of Protected Areas, <%= Date.today.strftime("%B %Y") %>. Available at: <a href="/">www.protectedplanet.net</a>
    </p>
  </section>

  <section class="info-box__column">
    <h2 class="info-box__header header-with-delimiter header-with-delimiter--bold">References</h2>
    <div class="info-box__body">
      <div class="reference">
        <h3 id="ref1" class="reference__title">(Ref 1)</h3>
        <p class="reference__body">UNEP-WCMC (2017). Global statistics from the World Database on Protected Areas (WDPA), <%= Date.today.strftime("%B") %> 2017. Cambridge, UK: UNEP-WCMC.</p>
      </div>
      <% if @pame_statistics && Rails.application.secrets.show_pame_statistics %>
        <div class="reference">
          <h3 id="ref2" class="reference__title">(Ref 2)</h3>
          <p class="reference__body">
          Coad, L. et al. (2015). Measuring impact of protected area management
          interventions: current and future use of the Global Database of
          Protected Area Management Effectiveness. Phil. Trans. R. Soc. B 370:
          20140281.
          </p>
          <a class="reference__link" href="http://dx.doi.org/10.1098/rstb.2014.0281"><i class="reference__link-icon fa fa-external-link-square"></i>External link</a>
        </div>
      <% end %>
    </div>
  </section>

  <!--
  <section class="info-box__column">
    <h2 class="info-box__header header-with-delimiter header-with-delimiter--bold">Downloads</h2>
    <div class="info-box__body info-box__body--no-padding">
      <div class="js-download-btn info-box__row">
        <div class="js-trigger tooltip__trigger info-box__link">
          <i class="info-box__link-icon fa fa-download"></i>Download this dataset
          <%= download_dropdown(@region.iso, 'general', (types rescue [:csv, :kml, :shp])) %>
        </div>
        <a href="<%= url_for(action: :show, iso: @region.iso, format: :pdf) %>" class="info-box__link">
          <i class="info-box__link-icon fa fa-file-pdf-o"></i>Download this page as a PDF
        </a>
      </div>

      <div class="<%= bem('info-box__row', 'no-border') %>">
        <button class="big-button" onclick="window.location.href='<%= search_url(region: @region.name, main: :region) %>'">
          <i class="fa fa-link big-button__icon"></i>Protected Area list of the country
        </button>
      </div>
    </div>
  </section>
  -->
</aside>
