// Matches the legacy ListingPage mixin's hardcoded pre-selectable filter ids —
// only these two filter-group ids are read from/written to the URL query string.
export const QUERY_STRING_FILTER_IDS = ['topics', 'types']
