<%= turbo_mount "SearchAreas", props: {
    config: config,
    endpointAutocomplete: search_autocomplete_path,
    endpointSearch: search_areas_path(search_term: "SEARCHTERM", filters: @filters)
} %> 