<%= content_for :seo, "Learn about and visualise the protected area #{@protected_area.name} (#{@protected_area.designation.try(:name)}), situated in #{@countries.map(&:name).join("; ") rescue "Areas Beyond National Jurisdiction"}" %>
<%= content_for :seo_image, tiles_url(type: "protected_area", id: @protected_area.wdpa_id, version: 1) %>
<%= content_for :page_title, @protected_area.name %>
<%= content_for :footer_classes, "fixed-footer" %>

<div class="container--fullscreen">
  <div class="u-only-desktop hero__top">
    <h1 class="hero__title--base hero__title">
      Official Record for <strong class="hero__link"><%= @protected_area.name %></strong> in
      <% if @countries.any? %>
        <% @countries.each_with_index do |country, i| %>
          <a class="hero__title hero__title--thin hero__link" href="<%= country_path(country.iso) %>">
            <%= country.name %>
          </a><%= ", " if i < @countries.length - 2 %><%= " and " if i == @countries.length - 2 %>
        <% end %>
      <% else %>
        <a class="hero__title hero__title--thin hero__link" href="#">
          Areas Beyond National Jurisdiction
        </a>
      <% end %>
    </h1>
    <h2 class="js-download-btn">
      <div class="u-only-desktop tooltip__trigger js-trigger link-with-icon link-with-icon--bold">
        <i class="link-with-icon__icon fa fa-download"></i>Download this site
        <%= download_dropdown(@protected_area.wdpa_id, 'protected_area', (types rescue [:csv, :kml, :shp])) %>
      </div>
    </h2>
  </div>

  <div class="u-only-mobile hero">
    <div class="hero__top">
      <h1 class="hero__title--base hero__title">
        Official Record for <strong class="hero__link"><%= @protected_area.name %></strong>
      </h1>
    </div>
    <div class="js-resizable map map--hero" id="map-mobile"
      data-bound-from="<%= map_bounds(@protected_area)['from'] %>"
      data-bound-to="<%= map_bounds(@protected_area)['to'] %>"
      data-wdpa-id="<%= @protected_area.wdpa_id %>"
      data-mobile-height=33
      >
    </div>
    <div class="disclaimer">
      The designations employed and the presentation of material on this map do not
      imply the expression of any opinion whatsoever on the part of the Secretariat
      of the United Nations concerning the legal status of any country, territory,
      city or area or of its authorities, or concerning the delimitation of its
      frontiers or boundaries.
    </div>
  </div>

  <div class="old-factsheet">
    <%= render partial: 'factsheet', locals: {protected_area: @protected_area} %>
  </div>

  <div class="old-map two-thirds map u-only-desktop" id="map"
    data-bound-from="<%= map_bounds(@protected_area)['from'] %>"
    data-bound-to="<%= map_bounds(@protected_area)['to'] %>"
    data-wdpa-id="<%= @protected_area.wdpa_id %>"
    data-scroll-wheel-zoom>
    <div class="map-disclaimer">
      The designations employed and the presentation of material on this map do not
      imply the expression of any opinion whatsoever on the part of the Secretariat
      of the United Nations concerning the legal status of any country, territory,
      city or area or of its authorities, or concerning the delimitation of its
      frontiers or boundaries.
    </div>
  </div>
</div>
