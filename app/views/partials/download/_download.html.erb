<download 
  button-text="<%= t('global.button.download') %>"

  <% if local_assigns.has_key? :classes %>
    class="<%= classes %>"
  <% end %>

  :options="<%= options %>"
  :text="<%= download_text %>"
>
</download>