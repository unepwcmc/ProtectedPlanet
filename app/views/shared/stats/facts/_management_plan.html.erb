<% if is_malaysia_pa? %>
  <section class="fact">
    <h2 class="fact__header">Management Plan</h2>

    <% management_plan_documents.each do |doc| %>
      <a href="<%= doc[:url] %>" class="info-box__link">
        <i class="info-box__link-icon fa fa-file-pdf-o"></i><%= doc[:name] %>
      </a>
    <% end %>
  </section>
<% end %>
