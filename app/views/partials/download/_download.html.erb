<download 
  <% if local_assigns.has_key? :classes %>
    class="<%= classes %>"
  <% end %>
  
  text="<%= t('global.button.download') %>"
>
<download-popup
  :options="<%= options %>"
/>
</download-popup>
</download>