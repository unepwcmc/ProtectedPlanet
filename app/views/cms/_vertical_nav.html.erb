<div class="vertical-nav">
  <h3 class="<%= bem('vertical-nav__element', :heading) %>">Sections</h3>

  <% Nokogiri::HTML(cms_block_content(:content)).css('h2').each do |section| %>
    <a href="#<%= section.xpath('a').attr('id').value rescue nil %>" class="vertical-nav__element"><%= section.content %></a>
  <% end %>
</div>
