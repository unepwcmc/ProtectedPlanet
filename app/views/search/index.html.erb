<div class="container--medium section-padding">
  <search
    endpoint="/search-results"
    placeholder="<%= t('global.placeholder.search-site') %>"
  ></search>

  <search-results
    endpoint="/search-results"
    no-results-text="<%= t('global.search.no-results') %>"
    results-text="<%= t('global.search.results') %>"
  ></search-results>
</div>
