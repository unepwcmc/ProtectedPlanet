<header class="hero--area-type <%= get_local_classes local_assigns %>">
  <div class="hero__container container">

    <div class="hero__content">
      <h1><%= title %></h1>

      <% if local_assigns.has_key? :summary %>
        <p class="hero__intro"><%= summary %></p>
      <% end %>

      <div class="hero__buttons">
        <%= link_to button_search_text, 
          button_search_url, 
          title: t('global.links.visit', link: button_search_url), 
          class: 'hero__button-1 button--primary' 
        %>

        <%= link_to button_download_text, 
          button_download_url, 
          title: t('global.links.download', link: button_download_url), 
          class: 'button--download' 
        %>
      </div>
    </div>

    <div>
      <div class="hero__stats circle--grey-dark sm-home-hero-stats">
        <i class="hero__icon icon--shield"></i>
        <div class="hero__stat-number">
          <counter 
            :total="30" 
            trigger="sm-home-hero-stats" 
            :decimal="1"
          ></counter>%
        </div>
        <span class="hero__stat-text"><%= hero_stat_text %></span>
      </div>
    </div>
  </div>
</header>