<header class="vw-layouts-partials-hero-small <%= get_local_classes local_assigns %>"
  <% if local_assigns.has_key? :image %>
    style="background-image: url('<%= image %>')"
  <% end %>
>
  <div class="vw-layouts-partials-hero-small__container">
    <h1>
      <%= title %>
    </h1>
  </div>
</header>
