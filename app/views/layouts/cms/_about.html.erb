<%= render partial: "./layouts/partials/hero-basic", locals: { title: @cms_page.label, classes: @cms_page.slug } %>

<section class="container section-padding">
  <%= cms_fragment_render("content") %>
</section>

<section class="section-padding--bottom">
  <%= render "partials/carousels/themes" %>
</section>

<section class="container section-padding--bottom">
  <%= render "partials/cards/resources" %>
</section>

<%= render "partials/ctas/live-report" %>
