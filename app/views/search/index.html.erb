<div class="vw-search">
  <%= turbo_mount "SearchSite", props: {
    categories: @categories,
    dataPageLoad: @results,
    endpoint: search_results_path,
    gaId: "Component: search site",
    placeholder: t('global.placeholder.search-site'),
    noResultsText: t('search.no-results'),
    resultsText: t('search.results')
  } %>
</div>
