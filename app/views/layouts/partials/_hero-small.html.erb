<header 
  class="hero--small bg-image-overlay--white <%= get_local_classes local_assigns %>"
  <% if local_assigns.has_key? :image %>
    style="background-image: url('<%= image %>')"
  <% end %>
>
  <div class="hero__container container">
    <div class="hero__content">
      <h1><%= title %></h1>
    </div>
  </div>
</header>