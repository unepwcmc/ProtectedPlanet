<download 
  button-text="<%= t('global.button.download') %>"

  <% if local_assigns.has_key? :classes %>
    class="<%= classes %>"
  <% end %>

  endpoint="/downloads"
  endpoint-poll="/downloads/poll"
  :options="<%= options %>"
  :text="<%= download_text %>"
>
</download>