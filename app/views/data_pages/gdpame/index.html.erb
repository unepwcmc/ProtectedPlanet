<%= render partial: "./layouts/partials/hero-basic", locals: thematic_and_data_area_hero_locals(@cms_page).merge(
  classes: "#{@cms_page.slug} bg-image-overlay--white"
) %>

<section class="spacer-medium--bottom">
  <%= render partial: 'partials/thematic_and_data_area/tabs', locals: {
    ga_id: "Slug: #{@cms_page.slug}",
    tabs_list: @tabs_list,
    tab_extras: [
      {
        tab_id: 1,
        partial: 'data_pages/gdpame/tab_content',
        replace_content: true,
        locals: {
          table_attributes: @table_attributes,
          filters: @filters,
          json: @json
        }
      }
    ]
  } %>
</section>

<%= render "partials/ctas/live-report" %>

<pame-modal :text="<%= t('thematic_area.pame.modal').to_json %>"></pame-modal>
